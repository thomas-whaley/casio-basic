public enum TokenType {
    PLUS,
    MINUS,
    MULTIPLY,
    DIVIDE,

    LOCATE,
    GET_KEY,

    CLEAR_TEXT,
    
    WHILE,
    WHILE_END,
    DO,
    LP_WHILE,
    FOR,
    TO,
    STEP,
    NEXT,
    IF,
    THEN,
    ELSE,
    IF_END,
    
    L_THAN,
    G_THAN,
    L_THAN_E,
    G_THAN_E,
    EQ_TO,
    NOT_EQ_TO,
    
    AND,
    OR,
    NOT,
    
    LPAREN,
    RPAREN,
    COMMA,
    
    NUM,
    TEXT,

    VAR_ASSIGN,
    VAR_NAME
}
