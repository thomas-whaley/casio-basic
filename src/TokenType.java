public enum TokenType {
    NUM,

    PLUS,
    MINUS,
    MULTIPLY,
    DIVIDE,

    L_THAN,
    G_THAN,
    EQ_TO,

    AND,
    OR,
    NOT,

    LPAREN,
    RPAREN,
    
    VAR_ASSIGN,
    VAR_NAME
}
