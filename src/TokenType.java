public enum TokenType {
    PLUS,
    MINUS,
    MULTIPLY,
    DIVIDE,
    
    WHILE,
    WHILE_END,
    FOR,
    TO,
    STEP,
    NEXT,
    IF,
    THEN,
    ELSE,
    IF_END,
    
    L_THAN,
    G_THAN,
    EQ_TO,
    
    AND,
    OR,
    NOT,
    
    LPAREN,
    RPAREN,
    
    NUM,
    VAR_ASSIGN,
    VAR_NAME
}
